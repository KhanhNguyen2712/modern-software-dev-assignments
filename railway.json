{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "uvicorn week3.server.transports.http_app:app --host 0.0.0.0 --port ${PORT}",
    "restartPolicyType": "on_failure",
    "restartPolicyMaxRetries": 10
  }
}
