# /docs-sync\n\nSync `week4/docs/API.md` with the current API implementation.\n\n## Input\n- `$ARGUMENTS` (optional): extra note for changelog summary.\n\n## Steps\n1. Review routes in `week4/backend/app/routers/` and schemas in `week4/backend/app/schemas.py`.\n2. Update `week4/docs/API.md` to reflect:\n   - Endpoints, methods, payloads, status codes.\n   - Validation and error responses for changed endpoints.\n3. Produce a concise route delta summary:\n   - Added routes\n   - Changed routes\n   - Removed routes\n4. Suggest test updates if docs and tests drift.\n\n## Output format\n- `Route delta` section\n- `Files changed` checklist\n- `Follow-up TODOs`\n\n## Safety\n- Keep changes idempotent and scoped to `week4/docs/API.md` unless route fixes are explicitly requested.\n