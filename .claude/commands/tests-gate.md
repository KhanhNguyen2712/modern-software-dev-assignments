# /tests-gate\n\nRun test and quality checks for week4 with an optional scope argument.\n\n## Input\n- `$ARGUMENTS` (optional): path or pytest `-k` expression.\n\n## Steps\n1. Change directory to `week4/`.\n2. If `$ARGUMENTS` is empty, run:\n   - `PYTHONPATH=. pytest -q backend/tests --maxfail=1 -x`\n3. If `$ARGUMENTS` is provided, run:\n   - `PYTHONPATH=. pytest -q backend/tests -k "$ARGUMENTS" --maxfail=1 -x`\n4. If tests pass, run:\n   - `ruff check backend/app backend/tests frontend/app.js`\n5. Summarize:\n   - Failing tests (if any), first error, and next fix step.\n   - If green, report "test gate passed" with touched modules.\n\n## Safety\n- Never run destructive git commands.\n- Only read/modify files under `week4/` unless explicitly asked.\n